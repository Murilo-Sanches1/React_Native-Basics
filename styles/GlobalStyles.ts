import { StyleSheet } from "react-native";

const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  titleText: {
    fontFamily: "merriSerif",
    fontSize: 20,
    color: "#fff",
  },
  paragraphText: {
    marginVertical: 8,
    lineHeight: 20,
  },
});

export default GlobalStyles;
